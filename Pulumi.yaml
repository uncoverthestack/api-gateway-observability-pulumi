name: api-observability-platform
runtime:
  name: python
  options:
    virtualenv: venv
description: >
  A reusable Pulumi template that provisions CloudWatch dashboards,
  automated alarms, and SNS alerting for any AWS API Gateway.
